<% if @waiver.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@waiver.errors.count, "error") %> prohibited this event from being saved:</h2>

    <ul>
    <% @waiver.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<dl class="waiver-info form-list">
  <fieldset>
    <legend><%= _('Waiver content') %></legend>
    <dt><%= f.label :title %></dt>
    <dd><%= f.text_field :title %></dd>

    <dt><%= f.label :body %></dt>
    <dd><%= f.text_area :body %></dd>
  </fieldset>
</dl>
