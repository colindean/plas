<% title = "New ticket for" + @event.name %>
<% content_for :title do %>
  <%=title %>
<% end %>

<h1><%=title %></h1>
<%= render 'form' %>

<%= link_to _('Back to %s') % @event.name, event_path(@event) %>
